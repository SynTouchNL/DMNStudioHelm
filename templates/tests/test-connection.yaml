apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "DMNStudioHelm.fullname" . }}-test-connection"
  labels:
    {{- include "DMNStudioHelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "DMNStudioHelm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
