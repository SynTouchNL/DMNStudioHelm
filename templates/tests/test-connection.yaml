apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dmnstudiohelm.fullname" . }}-test-connection"
  labels:
    {{- include "dmnstudiohelm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dmnstudiohelm.fullname" . }}:{{ .Values.backend.service.port }}']
  restartPolicy: Never
