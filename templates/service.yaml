apiVersion: v1
kind: Service
metadata:
  name: {{ include "dmnstudiohelm.fullname" . }}
  labels:
    {{- include "dmnstudiohelm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dmnstudiohelm.selectorLabels" . | nindent 4 }}
