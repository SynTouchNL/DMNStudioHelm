#Backend. Frontend below
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-backend
  labels:
    {{- include "dmnstudiohelm.labels" . | nindent 4 }}
spec:
  {{- if not .Values.backend.autoscaling.enabled }}
  replicas: {{ .Values.backend.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "dmnstudiohelm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "dmnstudiohelm.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        instancename: {{ .Values.backend.image.name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "dmnstudiohelm.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.backend.image.repository | default .Values.global.image.repository }}/{{ .Values.backend.image.name }}:{{ .Values.backend.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.backend.service.port }}
              protocol: TCP
          {{- with .Values.backendlivenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.backend.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.backend.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.backend.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          env:
            # ============================================================
            # Quarkus JDBC
            # ============================================================
            - name: QUARKUS_DATASOURCE_JDBC_URL
              value: {{ .Values.backend.config.QUARKUS_DATASOURCE_JDBC_URL }}
            - name: QUARKUS_DATASOURCE_USERNAME
              value: {{ .Values.backend.config.QUARKUS_DATASOURCE_USERNAME }}
            - name: QUARKUS_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                 name: {{ .Values.backend.config.secretfile }}
                 key: QUARKUS_DATASOURCE_PASSWORD

            # ============================================================
            # Quarkus OIDC
            # ============================================================
            - name: QUARKUS_OIDC_AUTH_SERVER_URL
              value: {{ .Values.backend.config.QUARKUS_REST_CLIENT_KEYCLOAK_API_URL | trimSuffix "/" }}/realms/{{ .Values.backend.config.QUARKUS_REST_CLIENT_KEYCLOAK_API_REALM }}

            - name: QUARKUS_OIDC_CLIENT_ID
              value: {{ .Values.backend.config.QUARKUS_OIDC_CLIENT_ID }}

            - name: QUARKUS_OIDC_CREDENTIALS_SECRET
              valueFrom:
                secretKeyRef:
                 name: {{ .Values.backend.config.secretfile }}
                 key: QUARKUS_OIDC_CREDENTIALS_SECRET

            # ============================================================
            # Quarkus Keycloak REST Client
            # ============================================================
            - name: QUARKUS_REST_CLIENT_KEYCLOAK_API_URL
              value: {{ .Values.backend.config.QUARKUS_REST_CLIENT_KEYCLOAK_API_URL }}

            - name: QUARKUS_REST_CLIENT_KEYCLOAK_API_REALM
              value: {{ .Values.backend.config.QUARKUS_REST_CLIENT_KEYCLOAK_API_REALM }}

            # ============================================================
            # Quarkus Logging
            # ============================================================
            - name: QUARKUS.LOG.LEVEL
              value: {{ .Values.backend.config.QUARKUS_LOG_LEVEL | default INFO }}

            # ============================================================
            # Operaton REST API
            # ============================================================
            - name: QUARKUS_REST_CLIENT_OPERATON_REST_API_UT_URL
              value: {{ .Values.backend.config.QUARKUS_REST_CLIENT_OPERATON_REST_API_UT_URL }}

            - name: QUARKUS_REST_CLIENT_OPERATON_REST_API_TEST_URL
              value: {{ .Values.backend.config.QUARKUS_REST_CLIENT_OPERATON_REST_API_TEST_URL }}

            - name: QUARKUS_REST_CLIENT_OPERATON_REST_API_ACC_URL
              value: {{ .Values.backend.config.QUARKUS_REST_CLIENT_OPERATON_REST_API_ACC_URL }}

            - name: QUARKUS_REST_CLIENT_OPERATON_REST_API_PROD_URL
              value: {{ .Values.backend.config.QUARKUS_REST_CLIENT_OPERATON_REST_API_PROD_URL }}

            # ============================================================
            # Operaton Basic Auth
            # ============================================================
            - name: QUARKUS_OPENAPI_GENERATOR_OPERATON_REST_API_JSON_AUTH_BASICAUTH_USERNAME
              value: {{ .Values.backend.config.QUARKUS_OPENAPI_GENERATOR_OPERATON_REST_API_JSON_AUTH_BASICAUTH_USERNAME }}

            - name: QUARKUS_OPENAPI_GENERATOR_OPERATON_REST_API_JSON_AUTH_BASICAUTH_PASSWORD
              valueFrom:
                secretKeyRef:
                 name: {{ .Values.backend.config.secretfile }}
                 key: QUARKUS_OPENAPI_GENERATOR_OPERATON_REST_API_JSON_AUTH_BASICAUTH_PASSWORD

      {{- with .Values.backend.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

---
#Frontend
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-frontend
  labels:
    {{- include "dmnstudiohelm.labels" . | nindent 4 }}
spec:
  {{- if not .Values.frontend.autoscaling.enabled }}
  replicas: {{ .Values.frontend.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "dmnstudiohelm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "dmnstudiohelm.labels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        instancename: {{ .Values.frontend.image.name }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "dmnstudiohelm.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: "{{ .Values.frontend.image.repository | default .Values.global.image.repository }}/{{ .Values.frontend.image.name }}:{{ .Values.frontend.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.frontend.service.port }}
              protocol: TCP
          {{- with .Values.backendlivenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.frontend.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.frontend.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
          - name: config-volume
            mountPath: /usr/share/nginx/html/config.json
            subPath: config.json
          {{- with .Values.frontend.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
      - name: config-volume
        configMap: 
          name: {{ .Release.Name }}-configmap
      {{- with .Values.frontend.extraVolumes }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
